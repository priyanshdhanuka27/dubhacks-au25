# Test Environment Configuration
NODE_ENV=test
PORT=3002

# JWT Configuration for tests
JWT_SECRET=091db82400fdc365fceb64c2fb1fbde4
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_EXPIRES_IN=7d

# AWS Configuration
AWS_REGION=us-west-2
AWS_ACCESS_KEY_ID=placeholder
AWS_SECRET_ACCESS_KEY=placeholder

# DynamoDB Configuration for tests
DYNAMODB_USERS_TABLE=eventsync-users-test
DYNAMODB_EVENTS_TABLE=eventsync-events-test
DYNAMODB_ENDPOINT=http://localhost:8000

# OpenSearch Configuration for tests
OPENSEARCH_ENDPOINT=https://test-opensearch-endpoint.us-east-1.aoss.amazonaws.com
OPENSEARCH_REGION=us-east-1
OPENSEARCH_INDEX_NAME=eventsync-events-test

# Bedrock Configuration for tests
BEDROCK_REGION=us-east-1
BEDROCK_KNOWLEDGE_BASE_ID=test-knowledge-base-id
BEDROCK_MODEL_ID=anthropic.claude-3-sonnet-20240229-v1:0
BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v2:0

# CloudWatch Configuration for tests
CLOUDWATCH_LOG_GROUP=/aws/lambda/eventsync-test
CLOUDWATCH_REGION=us-east-1

# CORS Configuration for tests
CORS_ORIGIN=http://localhost:3000

# Rate Limiting for tests
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Search Configuration for tests
SEARCH_MAX_RESULTS=10
SEARCH_DEFAULT_RADIUS=25

# Calendar Configuration for tests
ICS_FILE_TTL=300